taskhub:
  notification:
    grpc:
      enabled: false
  security:
    jwt-secret: taskhub-test-only-signing-key-at-least-32-bytes
    token-ttl: PT1H
  cors:
    allowed-origins: http://localhost:3000
