server:
  port: ${TASKHUB_API_PORT:8080}

spring:
  datasource:
    url: ${TASKHUB_DB_URL}
    username: ${TASKHUB_DB_USERNAME}
    password: ${TASKHUB_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
